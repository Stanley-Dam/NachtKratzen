/**
 * A packet that syncs the hider count all the player UI's
 */
class HiderCountSync {
    constructor(hiderAmount) {
        this.hiderAmount = hiderAmount;
    }
}

module.exports = HiderCountSync;